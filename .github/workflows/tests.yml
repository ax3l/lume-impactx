name: Run tests

on: [push, pull_request]

jobs:
  test:
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest]
        python-version: ["3.11", "3.12", "3.13"]
    steps:
      - uses: actions/checkout@v4
        with:
          # setuptools-scm needs the full history to derive a version
          fetch-depth: 0
      - name: Install uv
        uses: astral-sh/setup-uv@v5
        with:
          enable-cache: true
          cache-dependency-glob: "**/pyproject.toml"
      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}
      - name: Install dependencies and package
        # `impactx` extra pulls the sequential-CPU `impactx-noacc` wheel from PyPI;
        # the accelerated conda-forge build is not needed to exercise the LUME layer.
        run: |
          uv sync --extra dev --extra impactx --no-editable
      - name: List Python package versions
        run: uv pip list
      - name: Run tests
        run: |
          uv run --no-sync pytest -q lume_impactx/tests
